FROM node:22-alpine AS frontend-builder

WORKDIR /app/dashboard/frontend

COPY dashboard/frontend/package*.json ./
RUN npm ci

COPY dashboard/frontend/ ./
ARG VITE_MAIL_DOMAIN=zenime.my.id
ENV VITE_MAIL_DOMAIN=${VITE_MAIL_DOMAIN}
RUN npm run build

FROM python:3.11-slim

RUN apt-get update && apt-get install -y --no-install-recommends \
    libpango-1.0-0 \
    libpangocairo-1.0-0 \
    libgdk-pixbuf-2.0-0 \
    shared-mime-info \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
RUN python -c "import aiosmtplib, fastapi, httpx, itsdangerous, pydantic, redis, slowapi, sqlalchemy, reportlab, openpyxl"

COPY dashboard/ dashboard/
COPY database/ database/
COPY mail_delivery/ mail_delivery/
COPY --from=frontend-builder /app/dashboard/static/dist dashboard/static/dist

EXPOSE 8080

CMD ["uvicorn", "dashboard.app:app", "--host", "0.0.0.0", "--port", "8080"]
